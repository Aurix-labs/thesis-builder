# Examples

## 比亚迪_002594.html

**角色：** Phase 3 HTML 生成的金标范例（reference template），**不是真实分析输出**。

**用途：**
- Phase 3 分批写新报告时，按 `<!-- ANCHOR:xxx -->` 锚点读对应区段做结构参考
- 验证 `scripts/verify_html.sh` 校验流程（应输出 20 PASS / 0 FAIL）
- 演示 Linear Terminal+ 设计语言的最终落地形态

**⚠️ 禁止：**
- 不要把文件内的财务数字、目标价、评分当成真实研究结论
- 文件内所有数据均为构造的演示值，仅服务于样式与结构对齐

**真实分析输出路径：** `output/<股票名>_<代码>/<YYYY-MM-DD>/report.html`（由 skill 在运行时按 Phase 1 → 2 → 3 生成）
